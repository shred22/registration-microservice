

serenity.take.screenshots = FOR_FAILURES

headless.mode = true

serenity {
  project.name = "Demo Project using Serenity and Cucumber"
  test.root = "com.registration.integration.test.steps"
  tag.failures = "true"
  linked.tags = "issue"
  restart.browser.for.each = scenario
}
